<div class="row-fluid margin-left">
	<h2>Recent</h2>
</div>
<div class="row-fluid margin-left">
	<% @recent_listings.pop(6).each do |recent_item| %>
		<div class="span2">
			<%= render 'recent_item', item: recent_item %>
		</div>
	<% end %> 
</div>
