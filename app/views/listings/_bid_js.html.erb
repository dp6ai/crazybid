<script>
$("#listing-timer-<%= listing.id %>").countdown({until:<%= (listing.duration - (Time.now - listing.start_date) ).to_i %> });

<% if user_signed_in? %>
	$("#bid-<%= listing.id %>").on("click", function(){
    $.post('/listings/<%= listing.id %>/bids', { user_id: "<%= current_user.id %>" })
	})
<% else %>

<% end %>


$.getJSON('/listings/active.json', function(listing) { 
		$.each(listings, function(i, listing) {
			$("#listing-timer-" + listing.id).appendTo($(listing.seconds_to_end));
			$("#listing-price-" + listing.id).appendTo($(listing.current_price));

		})
}


// [
// {"id":6,"current_price":1,"duration":86400},
// {"id":7,"current_price":1,"duration":86400},
// {"id":8,"current_price":1,"duration":86400},
// {"id":9,"current_price":2,"duration":86410},
// {"id":10,"current_price":8,"duration":86470},
// {"id":11,"current_price":1,"duration":86400},
// {"id":12,"current_price":13,"duration":86544},
// {"id":13,"current_price":13,"duration":86520},
// {"id":14,"current_price":95,"duration":86850}
// ]





</script>

