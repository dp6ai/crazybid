<div class="well well-front recent-item">
  <div class="item-image">
    <a href="/listings/<%=item.id%>">
      <%= image_tag(item.photo.url(:square), :class => "img-responsive", :alt => "Responsive image") %> 
    </a>
  </div>
  <h2 class="recent-title"><%= item.title %></h2>
  <ul class="unstyled coming-recent-list">
    <li class="recent-bid coming-recent coming-recent-price">Winning Bid: <strong ><%= number_to_currency((item.current_price) /100.0, unit: '£') %></strong></li>
    <li class="recent-winning coming-recent">Winner: <strong ><%= item.current_winner %></strong></li>
  </ul>
</div>

<%= render 'bid_js', listing: item  %>
